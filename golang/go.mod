module github.com/example/supply-chain-test

go 1.22

// go.sum is auto-generated and records SHA256 hashes for all modules.
// Always commit both go.mod and go.sum to version control.
// Verify integrity: go mod verify

require (
    // Example: pin to exact version
    // golang.org/x/net v0.24.0
)
